Config {
  template = "%cpu% | %coretemp% | %memory% | %swap% | %disku% %diskio% | %dynnetwork% | %wifi% | %apricorn% | %ssh% }{ %uptime% | %date% | %battery% | %kbd% | %locks%",
  sepChar = "%",
  alignSep = "}{",
  commands = [ Run Cpu      [ "--template", "Cpu: <total>%"
                            , "--Low",      "50"
                            , "--High",     "85"
                            , "--low",      "lightgreen"
                            , "--normal",   "orange"
                            , "--high",     "red" ] 10

             , Run CoreTemp [ "--template", "Temp:<core0>"
                            , "-L", "40", "-H", "60", "-l", "lightblue", "-n", "gray90", "-h", "red"] 50

             , Run Memory  [ "--template", "Mem: <usedratio>%"
                           , "--High",     "80"
                           , "--low",      "lightgreen"
                           , "--normal",   "orange"
                           , "--high",     "red" ] 10

             , Run Swap   [ "--template", "Swp: <usedratio>%"
                          , "--Low",      "20"
                          , "--High",     "60"
                          , "--low",      "lightgreen"
                          , "--normal",   "orange"
                          , "--high",     "red" ] 10

             , Run DiskU  [ ("/", "/ <usedp>% used") ] [] 20

             , Run DiskIO [ ("/", "R <read> W <write>") ] [] 10

             , Run DynNetwork [] 10

             , Run Date    "%F (%a) %I:%M%p" "date" 10

	     , Run Kbd     [ ("us(dvorak)", "DV")
                           , ("us",         "US") ]

             , Run Locks

             , Run Uptime [] 10

             , Run Battery [] 10

             , Run CommandReader "/home/morgan/scripts/test-apricorn.sh" "apricorn"

             , Run CommandReader "/home/morgan/scripts/show-wifi.sh" "wifi"

             , Run CommandReader "/home/morgan/scripts/show-ssh.sh" "ssh"
             ]
}
